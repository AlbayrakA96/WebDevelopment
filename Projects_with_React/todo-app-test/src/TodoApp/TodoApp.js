import React from "react";

const TodoApp = (props) => {
  //   handleChange = (event) => {};
  return (
    <div>
      {/* <input type="text" onChange={this.handleChange}></input> */}
      <output></output>
    </div>
  );
};

export default TodoApp;
